package screenmatch.calculos;

//utilizando interface porque não podemos herdar de outra classe
public interface Classificavel {
    int getClassificacao(); //quem for usar a interface Classificacao vai ter que usar o getClassificacao
}
